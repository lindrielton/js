//let valores = [8,1,7,4,2,9]
//for(pos=0; pos < valores.length; pos++ ){
  //  console.log(`a posiçao ${pos} tem os valores ${valores[pos]}`)
//}
let num = [1,2,3,4,5,6]
for(let pos in num){
    console.log(`a posiçao ${pos} tem ${num[pos]}`)
}